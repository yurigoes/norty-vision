import { PageSkeleton } from "../../../components/Skeleton";

/** Enquanto a tela carrega. Ver `components/Skeleton.tsx`. */
export default function Loading() {
  return <PageSkeleton variant="split" />;
}
