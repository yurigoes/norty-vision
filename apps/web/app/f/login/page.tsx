"use client";

import { SupplierLoginForm } from "../SupplierLoginForm";

// Login genérico: deriva o slug do subdomínio (zito.yugochat.com.br).
export default function SupplierLogin() {
  return <SupplierLoginForm />;
}
